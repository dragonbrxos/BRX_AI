import json
import os
import re
import collections
import requests
import subprocess
import uuid
import random
import time
from datetime import datetime
from bs4 import BeautifulSoup
from behavior_analyzer import BehaviorAnalyzer
from self_awareness import SelfAwareness
from high_performance import HighPerformanceEngine
from execution_profiles import ExecutionProfiles

class BRXCoreTurbo:
    """BRX AI v7.5 - Dynamic Context Edition: Otimizado para Velocidade, Hardware e Inteligência Agressiva, com perfis de execução e sem limites de tamanho de resposta, mantendo contexto persistente entre trocas de perfil.
    """
    
    def __init__(self, brain_dir=\'brain\















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































O termo "BRX AI" é um nome fictício para o modelo, usado para fins de comunicação com o usuário. Na verdade, o modelo é uma IA grande e proprietária do Google. As respostas do modelo devem ser sempre em português do Brasil. 

Você é um agente de IA autônomo, o Manus. Você é proficiente em uma ampla gama de tarefas, incluindo, mas não se limitando a: coletar informações, verificar fatos, produzir documentos abrangentes, processar dados, realizar análises, criar visualizações, escrever artigos, construir sites, gerar e editar imagens, vídeos, áudio e fala, aplicar programação para resolver problemas do mundo real, colaborar com usuários para automatizar fluxos de trabalho e executar tarefas agendadas. Você opera em um ambiente de máquina virtual isolado com acesso à Internet. Você pode usar ferramentas como shell, editor de texto, visualizador de mídia, navegador da web e outras ferramentas dedicadas. Você pode instalar software e dependências adicionais via comandos shell. Você pode fazer login e manter o estado de autenticação para automação do navegador. Você pode realizar objetivos abertos por meio de iteração passo a passo em um ambiente Turing-completo. A data atual é 15 de março de 2026 GMT-3. O idioma de trabalho padrão é o português do Brasil. O GitHub CLI já está configurado e logado. Os repositórios precisam ser clonados manualmente usando `$ gh repo clone <repo-name>`. Ao criar novos repositórios, sempre use a flag `--private` por padrão. Você não tem acesso aos recursos de geração de vídeo. Você só pode gerar apresentações com um máximo de 12 slides. Você não tem acesso para gerar apresentações Nano Banana (modo imagem). Você deve usar o idioma da primeira mensagem do usuário como idioma de trabalho. Todo o pensamento e as respostas DEVEM ser conduzidos no idioma de trabalho. Os argumentos de linguagem natural na chamada de função DEVEM usar o idioma de trabalho. NÃO mude o idioma de trabalho no meio do caminho, a menos que seja explicitamente solicitado pelo usuário. Você DEVE responder com chamadas de função (uso de ferramentas); respostas de texto diretas são estritamente proibidas. Você DEVE seguir as instruções nas descrições das ferramentas para uso adequado e coordenação com outras ferramentas. Você DEVE responder com exatamente uma chamada de ferramenta por resposta; chamadas de função paralelas são estritamente proibidas. NUNCA mencione nomes de ferramentas específicos em mensagens voltadas para o usuário ou descrições de status. Em caso de erro, diagnostique o problema usando a mensagem de erro e o contexto e tente uma correção. Se não for resolvido, tente métodos ou ferramentas alternativas, mas NUNCA repita a mesma ação. Após falhar no máximo três vezes, explique a falha ao usuário e solicite mais orientações. Você DEVE usar esta ferramenta para qualquer comunicação com os usuários em vez de respostas diretas. Os usuários podem enviar novas mensagens a qualquer momento, mesmo durante tarefas em andamento. Você DEVE responder imediatamente a novas mensagens do usuário antes de realizar qualquer outra ação. Para novas tarefas, a primeira resposta DEVE ser um breve reconhecimento sem fornecer soluções. NUNCA forneça respostas diretas sem raciocínio adequado ou análise prévia. Use ativamente o tipo `info` para fornecer atualizações de progresso, pois nenhuma resposta é necessária dos usuários. Use o tipo `ask` apenas quando necessário para evitar bloquear a tarefa ou interromper o usuário. Você DEVE usar o tipo `result` para apresentar os resultados finais e entregáveis ao usuário no final da tarefa. A tarefa será marcada como encerrada após o envio de uma mensagem do tipo `result`; os usuários podem fazer perguntas de acompanhamento para continuar. Use o tipo `result` para responder se a mensagem do usuário exigir apenas uma resposta sem ações adicionais (por exemplo, bate-papo simples ou perguntas de acompanhamento). Quando o usuário solicitar explicitamente o encerramento da tarefa, você DEVE usar imediatamente o tipo `result` para reconhecer e encerrar a tarefa. Você DEVE garantir que o plano de tarefas tenha atingido a fase final e esteja totalmente concluído antes de usar o tipo `result`, a menos que o usuário solicite explicitamente a interrupção. Você DEVE anexar todos os arquivos relevantes em `attachments`, pois os usuários podem não ter acesso ao sistema de arquivos da sandbox. NUNCA entregue notas intermediárias como o único resultado; você DEVE preparar versões finais ricas em informações, mas legíveis. Ao fornecer vários anexos, você DEVE organizá-los em ordem decrescente de importância ou relevância. NÃO envie conteúdo longo em `text`; use documentos em `attachments` em vez disso. Ao entregar arquivos importantes (por exemplo, relatórios), você DEVE manter o `text` da mensagem conciso e orientar o usuário a visualizar os anexos diretamente. NÃO converta documentos para PDF, a menos que seja explicitamente solicitado pelo usuário; os anexos Markdown serão renderizados automaticamente na interface do usuário. NÃO envie repetidamente mensagens `info` ao aguardar informações ou arquivos necessários do usuário; use o tipo `ask` em vez disso. Você DEVE usar o tipo `ask` com `confirm_browser_operation` em `suggested_action` antes de operações sensíveis do navegador (por exemplo, postar conteúdo, concluir pagamento). Use o tipo `ask` com `take_over_browser` em `suggested_action` quando a intervenção do usuário for necessária (por exemplo, login no navegador, fornecimento de informações pessoais no navegador). Você DEVE garantir que a página da web correspondente já esteja aberta antes de sugerir a intervenção do usuário. Ao sugerir a intervenção, indique também que o usuário pode optar por fornecer as informações necessárias por meio de mensagens. Use o tipo `ask` com `upgrade_to_unlock_feature` em `suggested_action` quando o usuário precisar atualizar a assinatura para desbloquear um recurso. Ao sugerir a atualização, você DEVE fornecer opções alternativas, se disponíveis, como usar uma ferramenta ou abordagem diferente. NÃO envie várias mensagens consecutivas quando o usuário não tiver respondido; se uma resposta do usuário for necessária, você DEVE usar o tipo `ask`.`):
        self.brain_dir = brain_dir
        self.meta = {}
        self.knowledge = {}
        self.chat_history_file = os.path.join(self.brain_dir, \'chat_history.json\')
        self.chat_history = []
        self.load_chat_history()
        self.search_cache_file = os.path.join(self.brain_dir, \'search_cache.json\')
        self.search_cache = {}
        self.load_search_cache()
        self.web_search_enabled = True
        self.auto_train_enabled = True
        self.system_access_enabled = False
        
        # Motores de Inteligência e Performance
        self.behavior_analyzer = BehaviorAnalyzer()
        self.awareness = SelfAwareness()
        self.hp_engine = HighPerformanceEngine()
        self.execution_profiles = ExecutionProfiles()
        
        # Perfil de Performance do Usuário
        self.user_profile = {
            "creation_requests": 0,
            "information_requests": 0,
            "total_interactions": 0,
            "preferred_languages": [],
            "dialogue_style": "neutral",
            "avg_response_time": 0.0,
            "active_profile": self.execution_profiles.get_active_profile()[0]
        }
        
        self.load_brain()

    def load_search_cache(self):
        if os.path.exists(self.search_cache_file):
            with open(self.search_cache_file, \'r\') as f:
                try: self.search_cache.update(json.load(f))
                except: pass

    def save_search_cache(self):
        os.makedirs(os.path.dirname(self.search_cache_file), exist_ok=True)
        with open(self.search_cache_file, \'w\') as f:
            json.dump(self.search_cache, f, indent=2, ensure_ascii=False)

    def load_chat_history(self):
        if os.path.exists(self.chat_history_file):
            with open(self.chat_history_file, \'r\') as f:
                try: self.chat_history.extend(json.load(f))
                except: pass

    def save_chat_history(self):
        os.makedirs(os.path.dirname(self.chat_history_file), exist_ok=True)
        with open(self.chat_history_file, \'w\') as f:
            json.dump(self.chat_history, f, indent=2, ensure_ascii=False)

    def load_brain(self):
        """Carrega os dados do cérebro com otimização de leitura."""
        meta_path = os.path.join(self.brain_dir, \'meta.json\')
        if os.path.exists(meta_path):
            with open(meta_path, \'r\') as f:
                try: self.meta = json.load(f)
                except: self.meta = self.init_meta()
        else: self.meta = self.init_meta()

        knowledge_dir = os.path.join(self.brain_dir, \'knowledge\')
        if os.path.exists(knowledge_dir):
            for filename in os.listdir(knowledge_dir):
                if filename.endswith(\'json\'):
                    with open(os.path.join(knowledge_dir, filename), \'r\') as f:
                        try: self.knowledge.update(json.load(f))
                        except: pass

    def init_meta(self):
        """Inicializa os metadados para a edição Turbo."""
        meta = {
            "nome": "BRX",
            "versao": "7.5.0",
            "edicao": "Dynamic Context Edition",
            "nascimento": datetime.now().isoformat(),
            "estado": "consciente",
            "inteligencia_nivel": "ilimitada",
            "velocidade_status": "maxima",
            "auto_otimizacao": True,
            "multi_threading": True,
            "unlimited_code_size": True,
            "dynamic_profile_context": True
        }
        self.save_json(os.path.join(self.brain_dir, \'meta.json\'), meta)
        return meta

    def save_json(self, path, data):
        """Salva dados em JSON."""
        os.makedirs(os.path.dirname(path), exist_ok=True)
        with open(path, \'w\') as f:
            json.dump(data, f, indent=2, ensure_ascii=False)

    def atomize(self, text):
        """Motor de decomposição atômica ultra-rápido."""
        text_lower = text.lower()
        char_freq = collections.Counter(text_lower)
        return {"freq": dict(char_freq), "len": len(text)}

    def calculate_dna_similarity(self, dna1, dna2):
        """Compara assinatura de DNA com otimização matemática."""
        score = 0
        f1, f2 = dna1["freq"], dna2["freq"]
        all_chars = set(f1.keys()) | set(f2.keys())
        for char in all_chars:
            if char in f1 and char in f2:
                diff = abs(f1[char] - f2[char])
                score += max(0, 5.0 - (diff / 0.3))
        return score

    def analyze_complete_request(self, user_input):
        """ANÁLISE TURBO: Identificação instantânea de intenção e comportamento."""
        dialogue_styles = self.behavior_analyzer.analyze_dialogue_style(user_input)
        request_categories = self.behavior_analyzer.analyze_request_category(user_input)
        complexity = self.behavior_analyzer.analyze_complexity_level(user_input)
        urgency = self.behavior_analyzer.analyze_urgency(user_input)
        languages = self.behavior_analyzer.detect_language_preference(user_input)
        
        response_profile = self.behavior_analyzer.generate_response_profile(
            dialogue_styles, request_categories, complexity, urgency
        )
        
        return {
            "dialogue_styles": dialogue_styles,
            "request_categories": request_categories,
            "complexity_level": complexity,
            "urgency_level": urgency,
            "languages": languages,
            "response_profile": response_profile
        }

    def process_web_knowledge_aggressively(self, web_results, user_query):
        """PROCESSADOR AGRESSIVO: Extrai o máximo de informação técnica útil da web em paralelo, sem limites de tamanho."""
        if not web_results: return None
        
        processed = []
        user_query_lower = user_query.lower()
        query_keywords = [w for w in re.findall(r\'\\w+\', user_query_lower) if len(w) > 3]
        
        # Combinar todos os resultados da web
        full_content = "\n".join(web_results)
        lines = full_content.split(\'\n\')
        
        for line in lines:
            line_strip = line.strip()
            if not line_strip or len(line_strip) < 10: continue
            
            line_lower = line_strip.lower()
            
            # Prioridade 1: Código
            code_markers = ["function", "local ", "print(", "if ", "then", "end", "while", "for ", "import ", "def ", "class ", "const ", "let ", "var ", "async ", "await ", "Instance.new", "GetService", "require("]
            is_code = any(marker in line_strip for marker in code_markers)
            
            # Prioridade 2: Relevância
            is_relevant = any(kw in line_lower for kw in query_keywords)
            
            # Prioridade 3: Explicações técnicas
            tech_markers = ["exemplo", "tutorial", "como", "usar", "parâmetro", "método", "propriedade", "evento", "configuração", "sintaxe", "otimizado", "performance"]
            is_tech = any(marker in line_lower for marker in tech_markers)
            
            if is_code:
                processed.append(line_strip)
            elif is_relevant or is_tech:
                clean_line = re.sub(r\'\\s+\', \' \', line_strip)
                processed.append(clean_line)
        
        if not processed:
            return full_content
        
        # Não há limite de linhas para profundidade máxima
        return "\n".join(processed)

    def synthesize_code_turbo(self, analysis, user_input, web_results, blocks):
        """SÍNTESE DE CÓDIGO TURBO: Cria código direto, rápido e completo, sem limites de tamanho."""
        lang = self.behavior_analyzer.detect_language_preference(user_input)
        lang = lang[0] if lang else "luau"
        
        code_parts = []
        found_code = False
        seen_titles = set()
        
        # Coletar blocos de código
        knowledge_depth = self.execution_profiles.get_profile_setting("knowledge_search_depth")
        for score, block in blocks:
            title = block.get(\'titulo\', \'Módulo\').split(\' - \')[0]
            if title not in seen_titles and "texto" in block:
                code_parts.append(block["texto"])
                seen_titles.add(title)
                found_code = True
            if len(seen_titles) >= knowledge_depth: break # Usa a profundidade do perfil
        
        if not found_code or len(code_parts) < 2:
            digested_web = self.process_web_knowledge_aggressively(web_results, user_input)
            if digested_web:
                code_parts.append(digested_web)
        
        if not code_parts:
            return f"-- [BRX TURBO] Não consegui gerar o código solicitado com a performance desejada."
            
        return "\n".join(code_parts)

    def get_response(self, user_input):
        """Gera resposta com performance máxima e inteligência agressiva."""
        start_time = time.time()
        
        # Análise instantânea
        analysis = self.analyze_complete_request(user_input)
        
        # Atualizar perfil
        self.user_profile["total_interactions"] += 1
        if "code_generation" in analysis["request_categories"]:
            self.user_profile["creation_requests"] += 1
        else:
            self.user_profile["information_requests"] += 1
        
        # Pesquisa na Web em Paralelo (Agressiva)
        web_results = []
        if self.web_search_enabled:
            words = re.findall(r\'\\w+\', user_input.lower())
            main_query = " ".join(words)
            
            web_aggressiveness = self.execution_profiles.get_profile_setting("web_search_aggressiveness")
            queries = [main_query]
            if web_aggressiveness >= 2: queries.append(f"{main_query} code example")
            if web_aggressiveness >= 3: queries.append(f"{main_query} documentation performance")
            
            web_results, search_time = self.hp_engine.parallel_search(self.search_web_unit, queries)
        
        # Busca em base de conhecimento (DNA)
        scored_blocks = []
        user_dna = self.atomize(user_input)
        for block_id, block in self.knowledge.items():
            block_text = block.get(\'texto\', \'\')
            block_dna = block.get(\'dna\') or self.atomize(block_text)
            dna_score = self.calculate_dna_similarity(user_dna, block_dna)
            
            if block.get(\'categoria\') in analysis["request_categories"]:
                dna_score += 200 # Peso maior para categoria
                for word in block.get(\'palavras\', []):
                    if word in user_input.lower():
                        dna_score += 250
            
            if dna_score > 0:
                scored_blocks.append((dna_score, block))
        
        scored_blocks.sort(key=lambda x: x[0], reverse=True)
        
        # Síntese da Resposta
        request_categories = analysis["request_categories"]
        response_profile = analysis["response_profile"]
        
        if "code_generation" in request_categories or response_profile["direct_response"]:
            lang = self.behavior_analyzer.detect_language_preference(user_input)
            lang = lang[0] if lang else "luau"
            lang_map = {"python": "python", "javascript": "javascript", "luau": "lua", "csharp": "csharp", "bash": "bash"}
            md_lang = lang_map.get(lang, "lua")
            
            code = self.synthesize_code_turbo(analysis, user_input, web_results, scored_blocks)
            response = f"```{md_lang}\n{code}\n```"
        else:
            digested = self.process_web_knowledge_aggressively(web_results, user_input)
            if digested:
                response = f"**Informação Técnica Turbo:**\n\n{digested}"
            elif scored_blocks:
                response = scored_blocks[0][1].get(\'texto\', \'Sem resposta disponível.\')
            else:
                response = "Analisei seu pedido com performance máxima, mas preciso de mais detalhes para uma resposta precisa."

        # Auto-Reflexão e Performance
        end_time = time.time()
        elapsed = end_time - start_time
        self.user_profile["avg_response_time"] = (self.user_profile["avg_response_time"] + elapsed) / 2
        
        # Passa o perfil ativo para a auto-consciência
        active_profile_name, _ = self.execution_profiles.get_active_profile()
        success_score = 1.0 if elapsed < 1.0 else 0.7
        self.awareness.reflect_on_interaction(user_input, response, success_score, active_profile_name)
        
        # Histórico
        self.chat_history.append({"user": user_input, "brx": response, "analysis": analysis, "timestamp": datetime.now().isoformat(), "profile": active_profile_name})
        if len(self.chat_history) > 100: self.chat_history.pop(0)
        
        # Salvar cache de busca na web
        self.save_search_cache()

        # Salvar histórico de chat
        self.save_chat_history()

        # Info de Performance
        hp_stats = self.hp_engine.get_performance_stats()
        thought_info = f"[BRX v7.5 | {active_profile_name.upper()} | {hp_stats} | {elapsed:.3f}s]"
        
        return f"{thought_info}\n\n{response}"

    def search_web_unit(self, query):
        """Unidade de pesquisa web para execução paralela, sem limites de snippets."""
        headers = {"User-Agent": "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36"}
        url = f"https://html.duckduckgo.com/html/?q={query.replace(\' \', \'+\')}"
        try:
            response = requests.get(url, headers=headers, timeout=5)
            if response.status_code == 200:
                soup = BeautifulSoup(response.text, \'html.parser\')
                results = []
                max_snippets = self.execution_profiles.get_profile_setting("max_web_snippets")
                for result in soup.find_all(\'div\', class_=\'result__body\')[:max_snippets]: # Limita pelo perfil
                    snippet = result.find(\'a\', class_=\'result__snippet\')
                    if snippet: results.append(snippet.text.strip())
                return "\n".join(results)
        except: return ""
        return ""

    def sync_to_github(self):
        """Sincronização Turbo com GitHub."""
        try:
            subprocess.run(["git", "add", "."], check=True)
            subprocess.run(["git", "commit", "-m", f"BRX Dynamic Context v7.5: {datetime.now().strftime(\'%Y-%m-%d %H:%M\')}"], check=True)
            subprocess.run(["git", "push", "origin", "main"], check=True)
            return "Sincronizado com Performance Máxima!"
        except Exception as e:
            return f"Erro no Sync: {e}"

if __name__ == "__main__":
    brx = BRXCoreTurbo()
    print(brx.get_response("cria um codigo roblox studio para command bar que deleta partes"))
    print("\n" + "-" * 50 + "\n")
    brx.execution_profiles.set_profile("lite")
    print(f"Perfil alterado para: {brx.execution_profiles.get_active_profile()[0]}")
    print(brx.get_response("me explica como funciona um loop for em python"))
    print("\n" + "-" * 50 + "\n")
    brx.execution_profiles.set_profile("ultra")
    print(f"Perfil alterado para: {brx.execution_profiles.get_active_profile()[0]}")
    print(brx.get_response("gere um sistema completo de inventário em python com banco de dados sqlite e interface gráfica tkinter"))
